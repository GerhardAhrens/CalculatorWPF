<UserControl
    x:Class="System.Windows.Calculator.ExpressionCalculatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:System.Windows.Calculator"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="80"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <Grid Margin="10">

        <Grid.RowDefinitions>

            <RowDefinition Height="Auto" />

            <RowDefinition Height="Auto" />

            <RowDefinition Height="*" />

        </Grid.RowDefinitions>

        <TextBox
            x:Name="ExpressionBox"
            Grid.Row="0"
            Margin="0,0,0,5"
            FontSize="22"
            KeyDown="ExpressionBox_KeyDown"
            Text="{Binding ExpressionText, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="1"
            Foreground="Red"
            Text="{Binding ErrorText}" />

    </Grid>
</UserControl>
